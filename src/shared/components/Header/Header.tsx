import React from 'react';

// STYLED COMPONENTS
import { StyledHeader, StyledLogo } from './Header.styles';

export function Header() {
  return (
    <StyledHeader>
      <StyledLogo />
    </StyledHeader>
  );
}
